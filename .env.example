# Docker Compose 환경 변수 설정 예제
# 이 파일을 .env로 복사한 후 실제 값으로 수정하여 사용하세요.
# cp .env.example .env

# Spring 프로파일
SPRING_PROFILES_ACTIVE=dev

# MariaDB 설정
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=customer_db
MYSQL_USER=customer_user
MYSQL_PASSWORD=customer_password

# Redis 설정
REDIS_PASSWORD=redis_password

# JWT 설정 (프로덕션에서는 반드시 변경하세요!)
JWT_SECRET=your-secret-key-here-please-change-in-production-minimum-32-characters
JWT_ACCESS_TOKEN_EXPIRATION=3600000    # 1시간 (밀리초)
JWT_REFRESH_TOKEN_EXPIRATION=86400000  # 24시간 (밀리초)

# 로깅 레벨
LOGGING_LEVEL=INFO
LOGGING_LEVEL_APP=DEBUG

# JPA 설정
JPA_SHOW_SQL=false

# 애플리케이션 포트 설정 (필요시 변경)
# APP_PORT=8080
# MARIADB_PORT=3306
# REDIS_PORT=6379
# KAFKA_PORT=9092
# KAFKA_UI_PORT=8090