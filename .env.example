# Docker Compose 환경 변수 설정 예제
# 이 파일을 .env로 복사하고 실제 값으로 변경하여 사용하세요
# cp .env.example .env

# Spring 프로파일
SPRING_PROFILES_ACTIVE=dev

# 데이터베이스 설정
MYSQL_ROOT_PASSWORD=your_strong_root_password_here
MYSQL_DATABASE=customer_db
MYSQL_USER=customer_user
MYSQL_PASSWORD=your_strong_mysql_password_here

# Redis 설정
REDIS_PASSWORD=your_strong_redis_password_here

# JWT 설정
JWT_SECRET=your_strong_jwt_secret_key_here_at_least_256_bits
JWT_ACCESS_TOKEN_EXPIRATION=3600000
JWT_REFRESH_TOKEN_EXPIRATION=86400000

# JPA 설정
JPA_SHOW_SQL=false

# 로깅 설정
LOGGING_LEVEL=INFO
LOGGING_LEVEL_APP=DEBUG

# 보안 주의사항:
# 1. 절대 이 파일의 실제 값을 git에 커밋하지 마세요
# 2. 프로덕션 환경에서는 반드시 강력한 비밀번호를 사용하세요
# 3. JWT_SECRET은 최소 256비트(32바이트) 이상의 랜덤 문자열을 사용하세요
# 4. 각 환경(dev, staging, prod)마다 다른 값을 사용하세요